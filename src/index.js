import { build } from "./app/server.js";

/**
 * Entry point for pre running state logic and building out
 * the application implementation.
 */
const start = () => {

    build();
}

// Invoke the start method.
start();